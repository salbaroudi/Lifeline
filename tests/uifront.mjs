

//Assume this scirpt starts, with an http request from the front end.
//A set of seeds (one or more) is handed over from the front end. We also get a state request: Store seeds


//To do: get http-server running.

//run jsipfs from console, and connect to a node outside the browser!

//Hand over chosen seed (post by user), with state selection.

//If "check seed" is selected,
  //if seed node is placed. User must send a message to other user
  //if seed node is not placed
      // place the node. and wait for message...
